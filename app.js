let pronombre = ['el', 'un'];
let adj = ['gran', 'genial'];
let nombre = ['mundo', 'universo'];
let dominio = ['net', 'es', 'us', 'io'];

     for (j=0; j<pronombre.length; j++) {
        for (i=0; i<pronombre.length; i++) {
            for (k=0; k<nombre.length; k++) {
                for (h=0; h<dominio.length; h++) {
                    console.log(pronombre[i]+adj[j]+nombre[k]+"."+dominio[h]);
}
}
}
}
